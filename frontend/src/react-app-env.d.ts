/// <reference types="react-scripts" />

type body = {
    firstName: string,
    lastName: string,
    status: string
}

type id = string;

type fetchBody ={
    firstName: string,
    lastName: string,
    status: string
    _id: string
}
